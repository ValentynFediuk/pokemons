import {Logo} from "@/components/ui/Logo.tsx";

export const Header = () => (
    <header className='sticky top-0 z-40 bg-darkGray p-4'>
        <header>
            <Logo />
        </header>
    </header>
)